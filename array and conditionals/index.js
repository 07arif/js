var numbers = [45, 68, 30, 40, 55];

// 1. get element by index
var element = numbers[1];

console.log(element);

// 2. set element value by index;
numbers[1] = 10;
numbers[2] = 20;
console.log(numbers);

// 3. find index of an element:
var positionIndex = numbers.indexOf(20);
console.log(positionIndex);
